# Projet Supabase (Settings > API dans le dashboard Supabase)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Clé publique VAPID (notifications push) -- peut réutiliser la même paire
# que pokedeals-saas, ou en générer une nouvelle (cf. README.md).
NEXT_PUBLIC_VAPID_PUBLIC_KEY=

# Clé service_role Supabase (Project Settings > API) -- SERVEUR uniquement,
# jamais NEXT_PUBLIC_. Utilisée par le webhook Stripe pour écrire l'état
# d'abonnement.
SUPABASE_SERVICE_ROLE_KEY=

# Abonnement payant (Stripe) -- MÊME COMPTE Stripe que pokedeals-saas (cf.
# README.md section 4 pour la création du produit/prix/coupons/webhook).
STRIPE_SECRET_KEY=sk_test_xxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxx
STRIPE_PRICE_ID=price_xxxxx

# ID du Price Stripe de l'abonnement PokéDeals (même compte Stripe) --
# nécessaire pour détecter automatiquement si un client a déjà PokéDeals
# actif et appliquer le tarif bundle réduit (cf. lib/stripe.ts). Visible
# dans pokedeals-saas/.env.local sous le nom STRIPE_PRICE_ID.
POKEDEALS_STRIPE_PRICE_ID=price_xxxxx

# URL absolue du site (nécessaire pour les redirections Stripe Checkout) --
# http://localhost:3000 en local, l'URL Vercel/le domaine en prod.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Testeurs beta (optionnel) : emails avec accès complet gratuit, sans passer
# par Stripe. Séparés par des virgules.
BETA_TESTER_EMAILS=
